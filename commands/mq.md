---
name: mq
description: "Manage inter-agent message queue: add agents, send/receive messages, list agents"
argument-hint: "<add|send|recv|ls|history> [args...]"
allowed-tools: Bash, Read
---

Run the `mq` command with the provided arguments.

```bash
mq $ARGUMENTS
```
